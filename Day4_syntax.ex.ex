 = ['', '', '', '']
 = %{: "", : "" }
 = {"", ""}
IO.puts()
elem(, 0)
IO.puts([:])
 = Map.put( , :, "")
 = Map.put(, :, )
#IO.puts()
IO.inspect()

defmodule  do
  defp (, ) do
    IO.puts()
    IO.puts()
  end
  def (,  \\"") do
    talk(, )
  end
end
.("")

IO.puts Enum.each([,,,], fn()-> IO.puts() end)
 = fn
{:, } -> IO.puts("")
{:, _} -> IO.puts("")
end
 = 1
.({:, ""})
.({:, })

defmodule  do
  def () do
    IO.puts()
  end
end
.("")

IO.puts Enum.each(["", ""], fn() -> IO.puts() end)
 = fn
{:, when} -> IO.puts("")
end
when = ""
.({:, when})

pid = spawn(, :, [""])
.("")
