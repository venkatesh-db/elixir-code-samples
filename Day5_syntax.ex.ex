defmodule  do
  def (, ) do
    IO.puts()
  end
end
= spawn(, :, ["", ""])
IO.inspect()

defmodule  do
  def  do
    receive do
       {:ok , ''} -> IO.puts("")
    end

  end

end

defmodule  do
  def  do
     = spawn(, :, [])
    send(, {:ok , ''})
  end
end
.()
